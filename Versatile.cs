using Task_one;

namespace Versatile; //разносторонний

public class Versatile
{
    private double side0; // сторона 1
    private double side1; // сторона 2
    private double side2; // сторона 3

    // Конструктор класса
    public Versatile (double side0, double side1, double side2)
    {
        this.side0 = side0;  
        this.side1 = side1;
        this.side2 = side2;
    }

    //метод который позволяет узнать тип треуголника;
    public void Length(double calculation)
    {
        calculation  ;
    }
}
